vue 5일차 수업 내용

리액트의 핵심
버츄얼돔 = 빠르다(리얼돔보다 빠르다)

jsx(javascript XML) = html인데 열면 닫아야하는 xml의 특성을 가지고 있다. (html은 안닫아도 됨)

render() 부분에 코드를 완성한다.

jsx에선
class==>className
for ==>htmlFor
value==>defaultValue
로 바꿔쓴다.

뷰에서 data는 리액트에서 props
this.$set는 this.setState()
props는 props
ref는 createRef()

redux와 vuex는 같은 것이다.

yarn은 npm을 대체하기위해 페이스북에서 만든 툴

크롬에서
React Developer Tools 추가
Redux DevTools 추가

vscode에서
es7 react/redux/graphql/react-native snippets 추가
babel es6/es7 추가

개발시 탭 사용을 자제하는게 좋다.
- 컴퓨터마다 탭 사이즈가 다르기 때문에

rest 연산자 vs spread 연산자
es6에서 spread 연산자(...)를 많이 쓰게 됨

... 가 =을 기준으로 왼쪽은 rest연산자
=을 기준으로 오른쪽에 있으면 ... spread연산자

react es6에서 중요한 것
화살표 함수
분해 할당
spread 연산자
class로 객체 생성
상속(extends)